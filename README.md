# HerCycleV2

A private menstrual cycle tracker. Built with Streamlit.

## Setup

```bash
pip install -r requirements.txt
streamlit run app.py
```

## Data

All cycle data is stored locally in `data/cycles.csv`. This folder is gitignored for privacy.
